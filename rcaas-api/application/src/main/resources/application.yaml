spring:
  datasource:
    name: rcaas
    url: jdbc:postgresql://postgresql:5432/apps
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
  main:
    lazy-initialization: true
  jmx:
    enabled: false
name: Rcaas UI Pipeline
on:
  push:
    branches: [ "master" ]
    paths:
      - rcaas-ui/**
  workflow_dispatch:
env:
  APP_NAME: rcaas-ui
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Build docker image
        run: docker build $APP_NAME --file $APP_NAME/Dockerfile --tag ${{ secrets.DOCKER_REGISTRY }}/$APP_NAME
      - name: Docker login
        uses: docker/login-action@v2
        with:
          registry: ${{ secrets.DOCKER_REGISTRY }}
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}
      - name: Docker push
        run: docker push ${{ secrets.DOCKER_REGISTRY }}/$APP_NAME
  deploy:
    needs: [ build ]
    runs-on: ubuntu-latest
    steps:
      - name: Deploy container
        run: |
          CONTAINER_ID=`curl -s \
            --request GET \
            --header "X-Auth-Token: ${{ secrets.DOCKER_TOKEN }}" \
            "https://api.scaleway.com/containers/v1beta1/regions/nl-ams/containers" \
            -d "{}" | jq -r --arg APP_NAME "$APP_NAME" '.containers[] | select(.name==$APP_NAME) | .id'`
          curl \
            --request POST \
            --header "X-Auth-Token: ${{ secrets.DOCKER_TOKEN }}" \
            "https://api.scaleway.com/containers/v1beta1/regions/nl-ams/containers/$CONTAINER_ID/deploy" \
            -d "{}" -v
